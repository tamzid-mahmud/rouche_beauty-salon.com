document.addEventListener('DOMContentLoaded', () => { const b = document.body, p = document.querySelector('.preloader'), h = document.querySelector('.header'), bar = document.querySelector('.progress'), t = document.querySelector('.toggle'), m = document.querySelector('.mobile'), c = document.querySelector('.cursor'); window.addEventListener('load', () => setTimeout(() => p?.classList.add('done'), 500)); const scroll = () => { h?.classList.toggle('scrolled', scrollY > 30); if (bar) { let x = document.documentElement.scrollHeight - innerHeight; bar.style.width = (x ? scrollY / x * 100 : 0) + '%' } }; addEventListener('scroll', scroll, { passive: true }); scroll(); const close = () => { t?.classList.remove('open'); m?.classList.remove('open'); b.classList.remove('lock') }; t?.addEventListener('click', (e) => {
    e.stopPropagation();
    const isOpen = m.classList.toggle('open');
    t.classList.toggle('open', isOpen);
    b.classList.toggle('lock', isOpen);
}); m?.querySelectorAll('a').forEach(a => a.addEventListener('click', close)); const io = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); io.unobserve(e.target) } }), { threshold: .12, rootMargin: '0px 0px -40px' }); document.querySelectorAll('.reveal').forEach(x => io.observe(x)); document.querySelectorAll('[data-year]').forEach(x => x.textContent = new Date().getFullYear()); if (c && matchMedia('(pointer:fine)').matches) addEventListener('mousemove', e => { c.style.left = e.clientX + 'px'; c.style.top = e.clientY + 'px' }); document.querySelectorAll('[data-magnetic]').forEach(el => { el.addEventListener('mousemove', e => { let r = el.getBoundingClientRect(); el.style.transform = `translate(${(e.clientX - r.left - r.width / 2) * .12}px,${(e.clientY - r.top - r.height / 2) * .12}px)` }); el.addEventListener('mouseleave', () => el.style.transform = '') }); document.querySelectorAll('[data-tilt]').forEach(el => { el.addEventListener('mousemove', e => { let r = el.getBoundingClientRect(), x = e.clientX / r.width - r.left / r.width - .5, y = e.clientY / r.height - r.top / r.height - .5; el.style.transform = `perspective(800px) rotateY(${x * 4}deg) rotateX(${-y * 4}deg) translateY(-4px)` }); el.addEventListener('mouseleave', () => el.style.transform = '') }) });
